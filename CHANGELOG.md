# Changelog

## 1.0.0

- Initial release.
- Drag a file from the TreeView, drop it into a document to insert its
  project-relative path at the cursor(s).
- Configurable prefix, quoting, and extension handling.
- Floating drag preview label with the file's tree-view icon.
- `filepath-dragdrop:toggle` command to enable/disable at runtime.
